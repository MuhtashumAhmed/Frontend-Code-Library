--- If want to customize close icon 
--- Make sure dialogContent have boolean showCloseButton false ok
--- Then render custom icon in DialogClose tag 


 <DialogContent className="lg:max-w-2xl! overflow-auto max-h-[95%] p-0 gap-0 font-poppins" showCloseButton={false}>
        <DialogClose asChild>
  <button className="absolute right-4 top-4 text-gray-500 hover:text-black">
    <X size={18} />
  </button>
</DialogClose>



---- Open from anywhere by Function 

  const dispatch = useDispatch();
 <Button
        className="primary-button text-sm! rounded-md! flex items-center gap-2 px-4! "
        onClick={() => dispatch(setCreateBranchModal(true))}
      >
        {" "}
        <CirclePlus /> Create New Branch
      </Button>
